/// <summary>Which map element the 2D view is editing - matches UDB's own mode split (Vertices / Linedefs / Sectors / Things).</summary>
public enum EditMode
{
	Vertices,
	Linedefs,
	Sectors,
	Things,
}
